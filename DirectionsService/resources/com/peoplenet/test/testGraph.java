package com.peoplenet.test;


import static org.junit.Assert.*;

import org.junit.Ignore;
import org.junit.Test;

public class testGraph {

	@Test
	@Ignore
	public void test() {
		fail("Not yet implemented");
	}
	
	//test that if a vertex exists but doesn't have any edges it is removed from the vertex list
	
	//test that if a vertex exists, at least one of it's edges have an edge associated with it
	
	//test that if an vertex is found to not have an edge, the corresponding direction is moved to false.
}
